<div class="row">

  <div class="col-md-4">
    {{#each model as |note|}}
      {{#link-to 'notes.note' note class="note-list-item"}}
        {{note.id}}
      {{/link-to}}
    {{/each}}
  </div>

  <div class="col-md-8">
    {{outlet}}
  </div>

</div>
